import React from 'react';

import UserProfile from '../UserProfile';

import './style.scss';

const Sidebar = () => {
  return (
    <div id='sidebar'>
      <UserProfile />
    </div>
  );
};

export default Sidebar;
